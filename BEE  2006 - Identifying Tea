/* 2006 - Identifying Tea*/

#include <stdio.h>
 
int main() {
 
    int n,m,t=0;
    scanf("%d",&n);
    for(int i=0;i<5;i++)
    {
        scanf("%d",&m);
        if(m==n)
        {
            t++;
        }
    }
    printf("%d\n",t);
 
    return 0;
}
