/*BEE 1047*/

#include <stdio.h>
int main()
{
    int s,e,sm,em,diff;
    scanf("%d %d %d %d",&s,&sm,&e,&em);
    diff=(e*60+em)-(s*60+sm);
    if(diff<=0)
    {
        diff=diff+24*60;
    }

    printf("O JOGO DUROU %d HORA(S) E %d MINUTO(S)\n",diff/60,diff%60);

    return 0;
}
