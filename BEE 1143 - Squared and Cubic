/* Squared and Cubic */

#include <stdio.h>
int main()
{
    int n,j=1;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        printf("%d ",j);
        printf("%d ",(j*j));
        printf("%d\n",(j*j*j));
        j+=1;
    }

    return 0;
}
