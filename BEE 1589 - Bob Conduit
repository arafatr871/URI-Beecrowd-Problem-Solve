/* Bob Conduit */

#include<stdio.h>
int main()
{
    int n,a,b,t;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d %d",&a,&b);
        t=a+b;
        printf("%d\n",t);
    }


    return 0;
}
